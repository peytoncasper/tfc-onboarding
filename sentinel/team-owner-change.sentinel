import "tfplan/v2" as tfplan

tfe_teams = filter tfplan.resource_changes as _, resource {
	resource.type == "tfe_team" and
	resource.change.after.name != tfplan.variables.team_owner.value
}

main = rule {
	length(tfe_teams) == 0
}